export enum EUserBroadcastingChannel {
    CANDLE = "candle",
    TICKER = "ticker",
    TRADE = "trade",
    ORDERBOOK = "orderbook",
}

export enum EUserBroadcastingAction {
    SUBSCRIBE = "subscribe"
}